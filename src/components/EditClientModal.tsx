import { useState } from 'react'
import Modal from './Modal'
import PhoneInput from './PhoneInput'
import type { Client } from '../types'

const field='w-full border border-slate-200 rounded-lg px-3 py-2 text-sm text-slate-700 focus:outline-none focus:ring-2 focus:ring-brand-500/30 focus:border-brand-500'
const label='block text-xs font-medium text-slate-500 mb-1.5'

export default function EditClientModal({client,onClose,onSave}:{client:Client;onClose:()=>void;onSave:(client:Client)=>void}){
  const [name,setName]=useState(client.name); const [type,setType]=useState<Client['type']>(client.type); const [primaryContact,setPrimaryContact]=useState(client.primaryContact)
  const [email,setEmail]=useState(client.email); const [phone,setPhone]=useState(client.phone); const [organisationNumber,setOrganisationNumber]=useState(client.organisationNumber??'')
  const [address,setAddress]=useState(client.address??''); const [serviceRequirements,setServiceRequirements]=useState(client.serviceRequirements??''); const [standardInstructions,setStandardInstructions]=useState(client.standardInstructions??'')
  const [reportDeliveryPreference,setReportDeliveryPreference]=useState(client.reportDeliveryPreference??''); const [agreedFees,setAgreedFees]=useState(client.agreedFees??''); const [communicationDetails,setCommunicationDetails]=useState(client.communicationDetails??''); const [error,setError]=useState('')
  const save=()=>{if(!name.trim()||!primaryContact.trim())return setError('Client name and primary contact are required.');if(email.trim()&&!/^\S+@\S+\.\S+$/.test(email.trim()))return setError('Enter a valid email address.');if(phone.trim()&&phone.replace(/\D/g,'').length<7)return setError('Enter a valid phone number.');onSave({...client,name:name.trim(),type,primaryContact:primaryContact.trim(),email:email.trim(),phone:phone.trim(),organisationNumber:organisationNumber.trim(),address:address.trim(),serviceRequirements:serviceRequirements.trim(),standardInstructions:standardInstructions.trim(),reportDeliveryPreference:reportDeliveryPreference.trim(),agreedFees:agreedFees.trim(),communicationDetails:communicationDetails.trim(),contactPeople:[{id:`${client.id}-CONTACT-1`,name:primaryContact.trim(),role:type==='Insurer'?'Claims Handler':type==='Solicitor Firm'?'Solicitor / Case Handler':'Primary Contact',email:email.trim(),phone:phone.trim()},...(client.contactPeople??[]).slice(1)],lastActivity:'Just now'})}
  return <Modal title="Edit client" description={`${client.id} · Organisation, communication and service settings.`} onClose={onClose} width="max-w-3xl"><div className="space-y-5">{error&&<p className="text-xs text-red-600 bg-red-50 border border-red-100 rounded-lg px-3 py-2">{error}</p>}<section><p className="text-xs font-semibold uppercase tracking-wide text-slate-400 mb-3">Organisation</p><div className="grid sm:grid-cols-2 gap-3"><div><label className={label}>Client name *</label><input className={field} value={name} onChange={e=>setName(e.target.value)}/></div><div><label className={label}>Client type</label><select className={field} value={type} onChange={e=>setType(e.target.value as Client['type'])}><option>Solicitor Firm</option><option>Insurer</option><option>Direct Instruction</option></select></div><div><label className={label}>Organisation number</label><input className={field} value={organisationNumber} onChange={e=>setOrganisationNumber(e.target.value)}/></div><div><label className={label}>Address</label><input className={field} value={address} onChange={e=>setAddress(e.target.value)}/></div></div></section><section className="border-t border-slate-100 pt-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400 mb-3">Primary contact</p><div className="grid sm:grid-cols-2 gap-3"><div><label className={label}>Contact name *</label><input className={field} value={primaryContact} onChange={e=>setPrimaryContact(e.target.value)}/></div><div><label className={label}>Email</label><input type="email" className={field} value={email} onChange={e=>{setEmail(e.target.value);setError('')}}/></div><div className="sm:col-span-2"><label className={label}>Phone</label><PhoneInput value={phone} onChange={v=>{setPhone(v);setError('')}}/></div></div></section><section className="border-t border-slate-100 pt-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400 mb-3">Service settings</p><div className="grid sm:grid-cols-2 gap-3"><div><label className={label}>Service requirements</label><textarea className={field} rows={3} value={serviceRequirements} onChange={e=>setServiceRequirements(e.target.value)}/></div><div><label className={label}>Standard instructions</label><textarea className={field} rows={3} value={standardInstructions} onChange={e=>setStandardInstructions(e.target.value)}/></div><div><label className={label}>Report delivery preference</label><input className={field} value={reportDeliveryPreference} onChange={e=>setReportDeliveryPreference(e.target.value)}/></div><div><label className={label}>Agreed fees</label><input className={field} value={agreedFees} onChange={e=>setAgreedFees(e.target.value)}/></div><div className="sm:col-span-2"><label className={label}>Communication details</label><textarea className={field} rows={2} value={communicationDetails} onChange={e=>setCommunicationDetails(e.target.value)}/></div></div></section></div><div className="flex justify-end gap-2 mt-5 pt-4 border-t border-slate-100"><button onClick={onClose} className="px-4 py-2 text-sm font-medium text-slate-600 hover:bg-slate-50 rounded-lg">Cancel</button><button onClick={save} className="px-4 py-2 text-sm font-medium text-white bg-brand-600 hover:bg-brand-700 rounded-lg">Save client</button></div></Modal>
}
